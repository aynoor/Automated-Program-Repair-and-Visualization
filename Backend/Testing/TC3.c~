#include "../SourceCodeFiles/average.c"
#include <stdio.h>
#include <string.h>

int main(int argc, const char** argv) {
	printf("%i\n", 3);
	int array[] = {10, 25, 335, 40, 5};
	if (83 == average(array)) {
		printf("%s\n", "P");
	} else {
		printf("%s\n", "F");
	}
	return 0;
}
